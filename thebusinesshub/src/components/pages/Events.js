import React, { Component } from 'react';
import ListingEvents from '../user/events/ListingEvents';

export default class Events extends Component {
  render() {
    return (
      <div>
        <ListingEvents />
      </div>
    );
  }
}
